{
  "devDependencies": {
    "parcel": "^2.0.0-rc.0",
    "typescript": "^3.9.10"
  },
  "dependencies": {
    "axios": "^0.22.0"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html"
  }
}
